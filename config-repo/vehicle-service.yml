spring:
  datasource:
    url:jdbc:postgresql:localhost://spms_vehicle
    username:postgres
    password:${DB_PASSWORD}

jpa:
  hibernate:
    ddl-auto: update
  show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.PostgreSQLDialect
